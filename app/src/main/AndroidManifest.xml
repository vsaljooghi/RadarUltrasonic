<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.home.vas.RadarUltrasonic">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.home.vas.RadarUltrasonic.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.home.vas.RadarUltrasonic.LogActivity"
            android:label="@string/title_activity_log"
            android:theme="@style/AppTheme" />
        <activity android:name="com.home.vas.RadarUltrasonic.AboutActivity" />
        <activity android:name="com.home.vas.RadarUltrasonic.HelpActivity" />
        <activity android:name="com.home.vas.RadarUltrasonic.LicenseActivity"></activity>
    </application>

</manifest>